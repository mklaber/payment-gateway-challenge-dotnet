# Payment Gateway Unit Tests

These are pretty straightforward to run. Just have a go:

```bash
dotnet test
```

These tests cover 100% of business logic in our main app. There are free tools to view this coverage information, but maybe just use dotCover?
